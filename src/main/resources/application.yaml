spring:
  application:
    name: SistemaDeInventario
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/inventario_db
    username: root
    password: root

    jpa:
      hibernate:
        ddl-auto: update

    sql:
      init:
        mode: always

server:
  servlet:
    context-path: /docs
